{
  "name": "@datastar-kit/examples",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter datastar-kit build && pnpm run build:only",
    "build:only": "rm -rf dist && tsc -p tsconfig.build.json",
    "check": "pnpm run typecheck && pnpm run build",
    "dev": "pnpm run dev:counter",
    "dev:append-list": "pnpm run build && node dist/dev-server.js append-list",
    "dev:counter": "pnpm run build && node dist/dev-server.js counter",
    "dev:hono-counter": "pnpm run build && node dist/dev-server.js hono-counter",
    "dev:hono-live-counter": "pnpm run build && node dist/dev-server.js hono-live-counter",
    "dev:live-counter": "pnpm run build && node dist/dev-server.js live-counter",
    "dev:search": "pnpm run build && node dist/dev-server.js search",
    "dev:todo-sync": "pnpm run build && node dist/todo-sync.js",
    "dev:tsx-counter": "pnpm run build && node dist/dev-server.js tsx-counter",
    "dev:validation-form": "pnpm run build && node dist/dev-server.js validation-form",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.3",
    "datastar-kit": "workspace:*",
    "hono": "^4.12.19",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
