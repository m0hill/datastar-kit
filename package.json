{
  "name": "ts-star",
  "version": "0.0.0",
  "description": "Exploratory Effect + Datastar framework prototype",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/vendor && cp vendor/datastar.js dist/vendor/datastar.js",
    "check": "pnpm run typecheck && pnpm run test",
    "check:example:counter": "pnpm run typecheck && pnpm run test:example:counter",
    "check:example:live-counter": "pnpm run typecheck && pnpm run test:example:live-counter",
    "check:example:runtime-counter": "pnpm run typecheck && pnpm run test:example:runtime-counter",
    "check:example:search": "pnpm run typecheck && pnpm run test:example:search",
    "check:example:tsx-counter": "pnpm run typecheck && pnpm run test:example:tsx-counter",
    "check:example:validation-form": "pnpm run typecheck && pnpm run test:example:validation-form",
    "check:examples": "pnpm run typecheck && pnpm run test:examples",
    "dev": "pnpm run dev:counter",
    "dev:counter": "pnpm run build && node dist/examples/dev-server.js counter",
    "dev:live-counter": "pnpm run build && node dist/examples/dev-server.js live-counter",
    "dev:runtime-counter": "pnpm run build && node dist/examples/dev-server.js runtime-counter",
    "dev:search": "pnpm run build && node dist/examples/dev-server.js search",
    "dev:tsx-counter": "pnpm run build && node dist/examples/dev-server.js tsx-counter",
    "dev:validation-form": "pnpm run build && node dist/examples/dev-server.js validation-form",
    "test": "vitest run",
    "test:example:counter": "vitest run test/example-counter.test.ts",
    "test:example:live-counter": "vitest run test/live-counter-example.test.ts",
    "test:example:runtime-counter": "vitest run test/runtime-counter-example.test.ts",
    "test:example:search": "vitest run test/search-example.test.ts",
    "test:example:tsx-counter": "vitest run test/example-tsx-counter.test.ts",
    "test:example:validation-form": "vitest run test/validation-form-example.test.ts",
    "test:examples": "vitest run test/example-counter.test.ts test/example-tsx-counter.test.ts test/search-example.test.ts test/live-counter-example.test.ts test/runtime-counter-example.test.ts test/validation-form-example.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "datastar",
    "effect",
    "sse",
    "hypermedia"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.67",
    "effect": "4.0.0-beta.67"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
